import { Box } from "@mui/material";
import { styled } from "@mui/system";

const FlexBetween = styled(Box)({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",});



export   default  FlexBetween;
// we use this very rarely so that we flex thingsinto  proper location
// from here we go to navbar and nav bar is the first thing we create